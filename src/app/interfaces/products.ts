export interface Product {
    createdAt: string
    product_name: string
    product_price: string
    product_adj: string
    product_material: string
    product_desc: string
    producer: string
    city: string
    country: string
    id: string
  }

export interface Visible {
  visible: boolean
}